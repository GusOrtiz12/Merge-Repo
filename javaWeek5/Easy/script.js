var maxArray = (arr) => {
    return Math.max(...arr);
};
    
 const first = [2, 4, 0];
 const second =  [1, 3, 5, 3, 2, 4];
 const third = [8, 1, 2];

console.log(maxArray(first));
console.log(maxArray(second));
console.log(maxArray(third));